# Test Runs — display-compare
auto-generated by playwright-test-runner.sh

## Pula features do nowych testów
1. ~~Scale verification (cm/px calculation)~~
2. ~~Preset bar search/filter~~ ✅ (search_filter.spec.ts)
3. Layout grid equal divisions
4. Workspace drag precision
5. AR mode tap placement accuracy
6. DeskView camera switching
7. ~~Monitor removal (X button)~~ ✅ (monitor_removal.spec.ts)
8. Max 6 monitors limit enforcement
9. Spec table column sorting
10. Overlay mode toggle

---

## Poprzednie uruchomienia

### 2026-04-05 04:15 UTC (cron:c4d736bb)
**Status:** ✅ ALL TESTS PASSING (5/5)

**Wyniki:**
- `test_monitor_selection_basic` ✅ PASS - select preset, verify counter
- `test_multi_monitor_selection` ✅ PASS - select 3 monitors, verify counter
- `test_overlay_opacity_slider` ✅ PASS - select monitors, use slider (range 0.3-3)
- `test_preset_bar_search` ✅ PASS - filter monitors by keyword
- `test_monitor_removal` ✅ PASS - select then attempt remove

**Fixes applied:**
1. **Fixed monitor names in tests** - Original tests used "Apple Studio", "LG UltraGear", etc. but actual UI shows generic names like "24\" FHD", "27\" QHD", "27\" 4K", "32\" 4K"
2. **Fixed slider fill value** - Slider range is 0.3-3, not 0-100. Changed fill('50') to fill('1.5')
3. **Fixed counter locator** - Used `.first()` to avoid strict mode violation (counter appears twice on page)
4. **Fixed selector syntax** - Changed invalid `span:text-matches` to valid `text=/pattern/`

**New tests added:**
- `monitor_removal.spec.ts` - tests selecting a monitor and attempting to remove it

**Notes:**
- Page starts with 2 monitors pre-selected (24" FHD and 27" FHD)
- Counter format: "X/6 selected" appears in header and "Popular monitors" section
- Slider in DeskView controls scale (0.3x to 3x)
